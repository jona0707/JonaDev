---
import Container from "./UI/Container.astro";
import Badge from "./utils/Badge.astro";
import BadgePrimary from "./utils/BadgePrimary.astro";
import { REDES } from "../data/redes";
---

<Container id="inicio">
  <div class="flex flex-col lg:flex-row items-center">
    <div class="order-2 lg:order-1 flex flex-col flex-1 justify-center">
      <div class="flex flex-col md:flex-row gap-1 items-start md:items-end">
        <h1 class="text-lg md:text-4xl font-bold dark:text-primary">
          hey, soy <span class="text-8xl dark:text-primary">Jona</span>
        </h1>
        <div class="md: mb-4">
          <Badge text="Disponible para trabajar" color="available" />
        </div>
      </div>
      <p class="text-lg mb-3 dark:text-primary">
        Estudiante de último semestre de Ingeniería en Tecnologías de la
        Información en la EPN<span class="text-xs">EC</span>. Especializado en
        el <span class="font-bold text-secondary dark:text-secondaryDark"
          >desarrollo de aplicaciones web</span
        > con 1 año y medio de experiencia.
      </p>
      <div class="flex gap-1 flex-wrap sm:flex-nowrap">
        {
          REDES.map(({ text, color, link, icon: Icon }) => (
            <BadgePrimary text={text} color={color} link={link}>
              <Icon />
            </BadgePrimary>
          ))
        }
      </div>
    </div>
    <img
      src="/Avatar.webp"
      alt="Jona's Avatar"
      class="w-48 md:w-full max-w-96 order-1 lg:order-2 lg:w-4/12 border-[15px] border-black rounded-full p-5 dark:border-[#2b2b2b] dark:bg-[#f9f9f9]"
    />
  </div>
</Container>
